.DS_Store
.idea/
.vscode/
*.log
node_modules/

# Tool/agent artefacts (not part of the repo)
.playwright-mcp/
repo-structure.md

# fwd:issue-fix runtime + local test install (skill is registered via .claude-plugin/plugin.json).
# No trailing slash on the .claude/* entries so they also match the symlinks
# that setup-worktree.sh creates inside worktrees (CC#28041 workaround).
.claude/skills
.claude/issue-loop
.trees/
.worktrees/
