# CONTEXT.md

Shared vocabulary for the `fwd-skills` repo. Keeps terminology consistent across skills, READMEs, and future ADRs.

## Vocabulary

_To be filled in as conventions emerge. Add entries when a term is used in more than one place and needs a single canonical definition._

<!-- Example entry:
### Skill
A self-contained capability registered via `.claude-plugin/plugin.json`, defined by a `SKILL.md` file with YAML frontmatter (`name`, `description`) and a markdown body. Loaded by Claude Code on demand based on the description.
-->
