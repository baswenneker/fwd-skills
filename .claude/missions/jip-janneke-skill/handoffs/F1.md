# F1 — Create skills/productivity/fwd:jip-janneke/SKILL.md

Created `skills/productivity/fwd:jip-janneke/SKILL.md` — a pure-markdown productivity skill that rewrites referenced text into plain, readable language (jip-en-janneketaal). Modelled the target-resolution table on `fwd:explain` (seven input forms plus explicit unresolvable path) and adopted the compact rule style of `fwd:caveman`. Frontmatter includes `name` (exactly `fwd:jip-janneke`), `argument-hint`, and `allowed-tools` with Read/Bash/Glob/Grep/WebFetch but no Write or Edit. The description contains both Dutch and English trigger phrases and explicitly disambiguates from `fwd:explain` (interactive walkthrough, does not rewrite) and `fwd:caveman` (compresses with abbreviations — opposite direction). All four rewrite rules (A: abbreviations written out at first use; B: no unexplained jargon; C: ASCII diagrams for structure; D: concise and organised), the meaning-preservation rule, the output contract (chat-only, language mirrors source, summary block In 't kort/In short, conditional abbreviations list for ≥4 distinct abbreviations), and the prompt-injection safety rule are all explicit and unambiguous. Risky scan returned ok. F2 (plugin.json + README) is deliberately untouched.

## Issues discovered

- The `fwd:explain` resolution table does not have an explicit "nothing resolves → error" row; one was added explicitly for `fwd:jip-janneke` to satisfy VC-2's unresolvable-path requirement.
- The "bare filename with extension" row needed a long find command — copied directly from `fwd:explain` to keep consistency (conservative choice).
- Mixed-language sources: the spec says "output language mirrors source language" but does not define behaviour for genuinely mixed sources. Conservative choice: follow the dominant language; noted in the skill's Step 4.
