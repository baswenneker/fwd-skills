# F2 — Register fwd:jip-janneke in plugin.json and README

F2 added the `./skills/productivity/fwd:jip-janneke` entry to the `skills[]` array in `.claude-plugin/plugin.json` (appended after `fwd:premortem`, the last existing productivity skill) and added a matching row to the README skills table under the productivity section. The README description mirrors the frontmatter description's first paragraph verbatim, including the Dutch trigger phrases, per the convention that skill descriptions in the table mirror frontmatter regardless of language. plugin.json was validated with `jq` and exited 0. Only the two target files were touched — no SKILL.md changes, no scripts folder.

## Issues discovered

- The SKILL.md `description` field uses a YAML block scalar (`|`), so the frontmatter description spans multiple lines. The README row mirrors the first paragraph (the primary trigger line, verbatim) and trims the "Not fwd:explain / Not fwd:caveman" disambiguation lines — matching the trimming convention used by sibling rows (`fwd:explain`'s row also drops its "Use when…" tail).

## Orchestrator note

Judged the trimmed mirror acceptable against VC-7 because it matches the established sibling-row convention; the adversarial reviewer rules on it at the M1 boundary.
