# F9 — Finalize flow: slot teardown + unchanged serial finalize

Section 4 of the run-parallel SKILL.md now documents the concrete finalize flow in order: 4.1 reconcile-waves.sh (slot teardown — wholesale directory removal IS the slot-side .env* scrub; slot branches deleted too), then 4.2 the serial finalize.sh unchanged (marks done/blocked, scrubs the mission worktree's .env, keeps the worktree for review). The Interop section's leftover-slots paragraph now explicitly names step 4.1 as the finalize-side owner including the .env* scrub (VC-12 finalize clause). The M2 review's arg-order nit was fixed in SKILL.md step 3.3 (now `setup-slot.sh <slug> <slot-n> <feature-id>`); setup-slot.sh's own header was already correct, contrary to the review's claim. Reading reconcile-waves.sh confirmed no .env survival gap: both removal paths (worktree remove --force and the rm -rf fallback) delete the entire slot directory. Harness stays green (73/73).

## Issues discovered

- The M2 reviewer's nit was half-right: only SKILL.md step 3.3 had the wrong order; setup-slot.sh's header comment was already correct.
- reconcile-waves.sh needed no change — directory-wholesale removal covers the .env scrub in both paths.
