# F3 — validate-artifacts.sh: DAG validation

Attempt 1 was killed by a network error after modifying the script but before committing. Attempt 2 reviewed the uncommitted work — the DAG validation logic was sound and complete (~106 added lines covering all four required checks) — then verified and committed it.

Checks implemented: (1) refs-exist — every id in depends_on must name a real feature, exits non-zero naming the offender; (2) explicit self-cycle detection; (3) Kahn-style iterative peeling for general cycle detection, naming a feature on the remaining cycle; (4) forward-milestone dep guard naming the violating edge. Backward compatible: features without depends_on, with depends_on: [], or fully v1 state.json all pass.

Verified with /tmp fixtures: v1 no-depends_on (0), valid DAG (0), missing ref (1), self-cycle (1), 2-cycle (1), forward-milestone dep (1), backward-milestone dep (0), plus this mission's own state.json (0).

## Issues discovered

- A missing ref also trips the Kahn peeling as an unpeelable node, so a second (cycle) error is reported alongside the root-cause ref error; both name the offender, ref error listed first.
- The script's commit step needs a git repo at the worktree path, so fixtures used a real `git init`; DAG validation runs before the git section, so exit-1 cases bail earlier.
