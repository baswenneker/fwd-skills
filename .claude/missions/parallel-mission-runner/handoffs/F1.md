# F1 — Document depends_on in the canonical schema (docs only)

Added `depends_on` documentation to the two canonical docs for `fwd:mission-run`. In REFERENCE.md an inline JSONC comment block was inserted in the `features[]` schema example explaining the field's purpose, authorship, consumer, and ignore-by-serial-runner semantics, plus a new Field notes bullet that covers the chain-semantics rule (absent = implicit predecessor dependency), backward compatibility with v1 plans, and the full write/consume/ignore split. In SKILL.md one sentence was added after the intro paragraph pointing users at `fwd:mission-run-parallel` for DAG-based parallel execution while affirming this serial runner ignores `depends_on` and remains the safe default. No scripts were touched.

## Issues discovered

- risky-scan.sh is in `skills/engineering/fwd:mission-run/scripts/`, not `fwd:git-commit/scripts/` as the spawn prompt suggested — the coder used the actual location.
