# M3 — Scrutiny review (Crash-safe + interop documented)

M3 is high quality and well-evidenced. The reviewer verified empirically rather than trusting PASS lines: ran tests/smoke-waves.sh (73/73, including Scenario 8 asserting the full crash→reconcile path — slot dir/branch removed, integrated FA work intact and still done with commit_sha retained, mission tree clean, serial reconcile output surfaced, FB re-runnable) and built independent /tmp fixtures confirming reconcile-waves.sh removes a slot worktree containing a live .env (scrubbed) plus its slot branch while leaving the mission branch intact. The serial scripts dir is byte-identical across the range (empty diff), so reconcile.sh and finalize.sh are genuinely unchanged; reconcile-waves.sh delegates via `exec bash $SERIAL_SCRIPTS/reconcile.sh` rather than reimplementing adopt/discard. Docs complete: CLAUDE.md adds the cross-skill-reference convention and names all three mission skills; the coder agent cites both orchestrators; the old "only thing that spawns the coder" line in REFERENCE.md is rewritten; a repo-wide sweep found no surviving exclusivity claim. The SKILL.md Interop section carries all three VC-12 runner-switching clauses verbatim. No defects found.

## Verdicts

- **VC-9: PASS** — slot worktrees + branches removed (glob cannot match the mission branch), genuine delegation to unchanged serial reconcile.sh, harness Scenario 8 covers the path (assertions 44–58), independent fixture reproduced.
- **VC-10: PASS** — SKILL.md §4: 4.1 reconcile-waves teardown (explicit .env* scrub note) then 4.2 unchanged serial finalize.sh via sibling path; .env scrub verified empirically.
- **VC-11: PASS** — CLAUDE.md convention + three-skill family; coder agent names both orchestrators (description + body); REFERENCE.md exclusivity claim rewritten; sweep clean.
- **VC-12: PASS** — all three interop clauses verbatim in SKILL.md lines 257–269 (serial ignores slot refs; parallel reconcile/finalize own cleanup; breaker + attempts shared).
