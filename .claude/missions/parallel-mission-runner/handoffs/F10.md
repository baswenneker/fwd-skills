# F10 — Docs coherence (cross-skill convention, dual-orchestrator coder)

Updated three doc files to satisfy VC-11. CLAUDE.md gained a cross-skill script-reference convention bullet in the Conventions section (`${CLAUDE_SKILL_DIR}/../<sibling-skill>/scripts/` — fwd:mission-run-parallel → fwd:mission-run is the repo's first instance; renaming the referenced folder breaks referrers, check dependents first) and the Agents paragraph now lists the mission family as three skills (plan / run / run-parallel, the latter sharing the serial runner's scripts and state format). agents/fwd-mission-coder.md had both its frontmatter description and opening body sentence updated to name both orchestrators (fwd:mission-run serially; fwd:mission-run-parallel into per-feature slot worktrees). fwd:mission-run/REFERENCE.md's "only thing that spawns the coder" phrase was reworded — each orchestrator is the sole spawner within its own run; both spawn the coder. Grep sweep accounted for all remaining "spawned by fwd:mission-run" hits: the two validator agent files are deliberately untouched per the PRD; README.md line 18 and fwd:mission-run/SKILL.md frontmatter are accurate self/context descriptions, not exclusivity claims. Harness green (73/73), G1/G2 pass.
