# F4 — Scaffold fwd:mission-run-parallel + registration

Created skills/engineering/fwd:mission-run-parallel/SKILL.md — the wave orchestrator skill — with frontmatter, explicit risk statement, autonomous-mode rules (matching the serial runner's voice), full wave flow (preflight → v1-refusal → reconcile-waves → per-wave loop → milestone validation → finalize), conflict policy (cherry-pick conflict consumes no attempt, pins serial_only; gate-fail consumes an attempt and pins culprit), the interop/runner-switching section required by VC-12 (serial runner ignores slot refs, slots cleaned by parallel reconcile/finalize, circuit breaker and attempts shared in state.json), and a script-references table pointing exclusively to ${CLAUDE_SKILL_DIR}/../fwd:mission-run/scripts/ for all shared scripts with no duplicated bodies. Registered the skill in .claude-plugin/plugin.json and added a matching row to the README skills table.

## Left undone (by design — later features)

- pick-wave.sh, setup-slot.sh, integrate-feature.sh (F5/F6), reconcile-waves.sh (F8), tests/smoke-waves.sh (F7): referenced from SKILL.md, land later.

## Issues discovered

- Cross-skill script coupling: renaming the fwd:mission-run folder breaks the `${CLAUDE_SKILL_DIR}/../fwd:mission-run/scripts/` references — documented in the SKILL.md Script references section (and already in mission.md trade-offs).
- Invoking the skill before F8 lands would hit a missing reconcile-waves.sh — acceptable for a scaffold-only feature mid-mission.
