# Handoff F6 — Coder-dieet: reading_list + rules inline + model/effort naar size

## In één oogopslag
De coder-spawn is afgeslankt. `fwd:mission-run/SKILL.md` stap 2.3 geeft de coder voortaan de feature's `reading_list` mee als volledige oriëntatie-scope, met de instructie "lees alléén dit; scan de repo niet opnieuw", neemt gematchte rule-bestanden inline op onder de ~200-regelgrens (anders de paden-lijst als fallback), en kiest model/effort naar de feature's `size` (S/M/L; bij ontbrekend veld het huidige gedrag). Het plan (`fwd:mission-plan/SKILL.md` stap 6) genereert `reading_list` (uit de file-by-file-tabel + rule-paden) en `size` (uit de sizing-regel) per feature. Beide velden zijn additief — oude plannen blijven geldig.

## Wat is er gewijzigd
- **`fwd:mission-run/SKILL.md` stap 2.3**: reading_list-bullet met "read only this", <200-regels-inline-regel voor rule_paths, en een "Model/effort by feature size"-richtlijn.
- **`fwd:mission-run/REFERENCE.md`**: `features[].reading_list` en `features[].size` als additieve schema-v6-velden (voorbeeld + veldnotities, analoog aan `depends_on`).
- **`fwd:mission-plan/SKILL.md` stap 6**: afleiding van `reading_list` en `size` per feature, expliciet additief.

## Bewijs
- `find skills agents -name '*.sh' | xargs bash -n` → exit 0 (geen scripts geraakt).
- grep op `reading_list`, `~200`, `read only this`, `Model/effort by feature size` in run/SKILL.md → treffers.
- grep op `reading_list` in plan/SKILL.md en `reading_list`/`size` in run/REFERENCE.md → treffers.

## Keuze
- `fwd:mission-plan/REFERENCE.md` bevat alleen de markdown-artefact-templates (mission.md, validation-contract.md), niet het state.json feature-schema — dat schema staat uitsluitend in `mission-run/REFERENCE.md`. Daar dus geen wijziging, consistent met hoe `rule_paths` eerder is behandeld.
- De "lager effort of lichter model"-richtlijn is bewust vaag (geen concreet model-ID), omdat de bestaande spawn-instructie ook geen model pint; een hardere keuze zou een nieuwe afspraak zijn.
